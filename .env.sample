# === GlitchLab ENV (project dir) ===
GLX_PKG=glitchlab
GLX_RUN=AMZ

# Outputs (must live inside repo root)
GLX_OUT=.glx/out
GLX_AUTONOMY_OUT=.glx/autonomy

# Mosaic params (required if M is enabled)
GLX_MOSAIC=hybrid
GLX_ROWS=96
GLX_COLS=160
GLX_EDGE_THR=0.25
GLX_DELTA=0.50
GLX_KAPPA=0.30
GLX_PHI=policy
GLX_POLICY=analysis/policy.json
